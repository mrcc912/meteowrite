var fs = require("fs");

exports.readFile = function() {
console.log("cabla");
fs.readFile('./sacbee_pubsys_stories.csv', function (err, data) {
  if (err) throw err;
  var str = String.fromCharCode.apply(null, new Uint16Array(data));
  console.log(str.split("\n").length);
});
}
